import 'package:fablearner_app/exports/presentation_exports.dart';
import 'package:fablearner_app/exports/common_exports.dart';

/* Future<void> saveScreen() async {
    // Add this code to your app's initState method
    final prefs = await SharedPreferences.getInstance();
    List<String> routes = prefs.getStringList('routes') ?? [];
    if (routes.isNotEmpty) {
      // Remove the current route from the list
      routes.removeLast();
      // Navigate to the last route in the list
      for (int i = routes.length - 1; i >= 0; i--) {
        Get.toNamed(routes[i]);
      }
    }
  } */
